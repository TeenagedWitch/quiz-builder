export enum BlockType {
  Heading = "heading",
  Question = "question",
  Button = "button",
  Footer = "footer",
}

export const allBlockTypes: BlockType[] = [
  BlockType.Heading,
  BlockType.Question,
  BlockType.Button,
  BlockType.Footer,
];
